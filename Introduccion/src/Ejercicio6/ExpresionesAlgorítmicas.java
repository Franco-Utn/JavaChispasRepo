package Ejercicio6;

public class ExpresionesAlgorítmicas {
    public static void main(String[] args) {
        // Convertir expresiones algebráicas en expresiones algorítmicas
        // Ejercicio: Convierte en expresiones algorítmicas las siguientes expresiones algebráicas. Coloca paréntesis solamente donde sean necesarios.
      /*
        double x,y,m,n,p,q,r,s,e,w;
        //a)
            double a;
            a = 3/2 + 4/3;
        //b)
            double b;
            b = 1 / (x - 5) - (3*x*y/4);
        //c)
            double c;
            c = 1/2 + 7;
        //d)
            double d;
            d = 7 + 1 / 2;
        //f)
        double f;
            f = m/n+p;
        //g)
        double g;
        g = m+n/(p-q);
        //h)
        double h;
        h = a*a/(b*b)+c*c/(d*d);
        //i)
        double i;
        i = (m+n/p)/(q-r/s);
        //j)
        double j;
        j = (3*a+b)/(c-(d+5*w)/(f+g/(2*h)));
        //k)
        double k;
        k = (a*a+2*a*b+b*b)/(1/(x*x))+2;
        //e)
        double e;
        e =  a*a/(b-c)+(d-e)/(f-g*h/j);
*/
    }
}
