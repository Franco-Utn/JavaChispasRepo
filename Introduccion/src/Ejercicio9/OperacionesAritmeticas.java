package Ejercicio9;

public class OperacionesAritmeticas {
    public static void main(String[] args) {
        /*
a) i + c:

El tipo de dato del resultado será int. Los operandos i (int) y c (char) se sumarán, y el resultado será un int debido a que int es de mayor precisión que char.
b) f - c:

El tipo de dato del resultado será float. Los operandos f (float) y c (char) se restarán, y el resultado será un float debido a que float es de mayor precisión que char.
c) d + f:

El tipo de dato del resultado será double. Los operandos d (double) y f (float) se sumarán, y el resultado será un double debido a que double es de mayor precisión que float.
d) d + i:

El tipo de dato del resultado será double. Los operandos d (double) y i (int) se sumarán, y el resultado será un double debido a que double es de mayor precisión que int.
e) i / f:

El tipo de dato del resultado será float. Los operandos i (int) y f (float) se dividirán, y el resultado será un float debido a que float es de mayor precisión que int.
f) s + j:

El tipo de dato del resultado será int. Los operandos s (short) y j (int) se sumarán, y el resultado será un int debido a que int es de mayor precisión que short.
g) d + j:

El tipo de dato del resultado será double. Los operandos d (double) y j (int) se sumarán, y el resultado será un double debido a que double es de mayor precisión que int.
h) s * c:

El tipo de dato del resultado será int. Los operandos s (short) y c (char) se multiplicarán, y el resultado será un int debido a que int es de mayor precisión que short.
i) d + c:

El tipo de dato del resultado será double. Los operandos d (double) y c (char) se sumarán, y el resultado será un double debido a que double es de mayor precisión que char.
j) b + c:

El tipo de dato del resultado será int. Los operandos b (byte) y c (char) se sumarán, y el resultado será un int debido a que int es de mayor precisión que byte.
k) b / c + s:

El tipo de dato del resultado será int. Los operandos b (byte), c (char), y s (short) se dividirán y sumarán, y el resultado será un int.
l) c + c:

El tipo de dato del resultado será int. Los operandos c (char) se sumarán, y el resultado será un int.
m) i + ln + d:

El tipo de dato del resultado será double. Los operandos i (int), ln (long) y d (double) se sumarán, y el resultado será un double.
n) ln + c:

El tipo de dato del resultado será long. Los operandos ln (long) y c (char) se sumarán, y el resultado será un long.
o) 5 / j:

El tipo de dato del resultado será int. Los operandos 5 (int) y j (int) se dividirán, y el resultado será un int.
p) 5.2 / j:

El tipo de dato del resultado será double. El operando 5.2 es un double, y se dividirá por j (int), y el resultado será un double.
q) i * f * 2.5:

El tipo de dato del resultado será float. Los operandos i (int), f (float), y 2.5 (double) se multiplicarán, y el resultado será un float.
r) ln * f * 2.5F:

El tipo de dato del resultado será double. Los operandos ln (long), f (float), y 2.5F (float) se multiplicarán, y el resultado será un double.
s) j - 4L:

El tipo de dato del resultado será long. Los operandos j (int) y 4L (long) se restarán, y el resultado será un long.
t) j - 4L * 2.5F:

El tipo de dato del resultado será float. Los operandos j (int), 4L (long), y 2.5F (float) se multiplicarán y restarán, y el resultado será un float.
u) b + 2.5 * i + 35F:

El tipo de dato del resultado será float. Los operandos b (byte), i (int), 2.5 (double), y 35F (float) se multiplicarán y sumarán, y el resultado será un float.
v) 'a' + b:

El tipo de dato del resultado será int. Los operandos 'a' (char) y b (byte) se sumarán, y el resultado será un int.
w) 'a' + c:

El tipo de dato del resultado será int. Los operandos 'a' (char) y c (char) se sumarán, y el resultado será un int.
x) c + 2:

El tipo de dato del resultado será int. Los operandos c

         */

    }
}
